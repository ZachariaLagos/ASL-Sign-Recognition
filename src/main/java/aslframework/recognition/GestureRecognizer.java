package aslframework.recognition;

/**
 * Interface for gesture recognition strategies.
 * Allows multiple implementations (MediaPipe, template matching, etc).
 */
public interface GestureRecognizer {
  // TODO: Define interface methods
}